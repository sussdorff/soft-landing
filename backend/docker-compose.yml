services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: softlanding
      POSTGRES_USER: softlanding
      POSTGRES_PASSWORD: softlanding
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
